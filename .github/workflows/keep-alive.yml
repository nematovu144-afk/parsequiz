name: Keep Render instance awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the live site
        run: curl -fsS -o /dev/null -w "HTTP %{http_code}\n" https://parsequiz.onrender.com/
